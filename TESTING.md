# SMIPE 統合テストシナリオ

## Week 7: YouTube移行後の統合テスト

このドキュメントは、SpotifyからYouTubeへの移行が正しく完了したことを確認するためのテストシナリオです。

---

## 前提条件

- ✅ すべてのDBマイグレーションが実行済み（12個）
- ✅ Google OAuth認証情報が設定済み（.env）
- ✅ YouTube API Keyが設定済み（.env）
- ✅ 開発サーバーが起動可能（`rails s`）

---

## テストシナリオ1: ホームページ（体験優先UX）

### 目的
ログイン不要で音楽体験が開始できることを確認

### 手順
1. ブラウザで `http://localhost:3000` にアクセス
2. ホームページが表示されることを確認
3. **プライマリCTA「音楽を聴く」ボタンが表示されていることを確認**
4. **セカンダリCTA「Googleでログイン」ボタンが表示されていることを確認**
5. 説明文「※ログインは地図への音楽投稿に必要です」が表示されていることを確認
6. フッターに「利用規約」「プライバシーポリシー」リンクが表示されていることを確認

### 期待結果
- ✅ ホームページが正常に表示される
- ✅ 「音楽を聴く」ボタンが目立つ位置にある（プライマリCTA）
- ✅ ログインボタンは控えめに表示される（セカンダリCTA）
- ✅ フッターリンクが機能する

---

## テストシナリオ2: プレイヤーページ（認証不要）

### 目的
ログインなしで音楽が再生できることを確認

### 手順
1. ホームページから「音楽を聴く」ボタンをクリック
2. プレイヤーページ（`/player`）に遷移することを確認
3. **日本のトレンド音楽が自動的に読み込まれることを確認**
4. 動画サムネイル、タイトル、チャンネル名が表示されることを確認
5. 再生ボタンをクリックして音楽が再生されることを確認
6. スキップボタン、前の曲ボタンが機能することを確認
7. シークバーで再生位置を変更できることを確認
8. **ログインプロンプトが表示されることを確認**（「地図に音楽を投稿するにはログインが必要です」）

### 期待結果
- ✅ 認証なしでプレイヤーにアクセスできる
- ✅ YouTube動画が正常に再生される
- ✅ トレンド音楽が自動読み込みされる
- ✅ 再生コントロールが全て機能する
- ✅ ログインプロンプトが適切に表示される

---

## テストシナリオ3: Google OAuth認証

### 目的
Google OAuthログインが正常に機能することを確認

### 手順
1. プレイヤーページまたはホームページで「Googleでログイン」をクリック
2. Googleログインページにリダイレクトされることを確認
3. Googleアカウントでログイン
4. SMIPEに戻ってきてログイン状態になっていることを確認
5. ヘッダーにプロフィール画像とユーザー名が表示されることを確認

### 期待結果
- ✅ Google OAuth認証が正常に完了する
- ✅ ユーザー情報（名前、メール、画像）がDBに保存される
- ✅ `provider: 'google'`でユーザーが作成される
- ✅ Spotifyトークン関連のエラーが発生しない

---

## テストシナリオ4: 利用規約・プライバシーポリシー

### 目的
法的ページが正しく表示されることを確認

### 手順
1. フッターまたはプレイヤーページのフッターから「利用規約」をクリック
2. `/terms`ページが表示されることを確認
3. **YouTube Data API v3利用に関する記述があることを確認**
4. **"Music provided by YouTube"アトリビューションがあることを確認**
5. 「プライバシーポリシー」をクリック
6. `/privacy`ページが表示されることを確認
7. Google OAuth、YouTube API、位置情報に関する記述があることを確認

### 期待結果
- ✅ 利用規約ページが正常に表示される
- ✅ プライバシーポリシーページが正常に表示される
- ✅ YouTube関連の記述が含まれている
- ✅ Google OAuth関連の記述が含まれている

---

## テストシナリオ5: 地図ページ（公開アクセス）

### 目的
ログインなしで地図ページにアクセスできることを確認

### 手順
1. ブラウザで `/map` にアクセス
2. 地図が表示されることを確認
3. 位置情報許可を求められた場合は許可
4. 現在地が地図上に表示されることを確認
5. **既存の音楽ピンが地図上に表示されることを確認**（サムネイル画像がマーカー）
6. ピンをクリックしてポップアップが表示されることを確認
7. ポップアップに曲名、チャンネル名、投稿者情報が表示されることを確認

### 期待結果
- ✅ 認証なしで地図ページにアクセスできる
- ✅ 地図が正常に表示される
- ✅ 音楽ピンが地図上に表示される
- ✅ ピンクリックで詳細情報が表示される

---

## テストシナリオ6: YouTube音楽検索

### 目的
YouTube音楽検索機能が正常に動作することを確認

### 手順
1. 地図ページ（`/map`）にアクセス
2. 検索フォームに曲名を入力（例: "紅蓮華"）
3. 検索ボタンをクリック
4. 検索結果が表示されることを確認
5. 各結果にサムネイル、タイトル、チャンネル名、再生時間が表示されることを確認
6. 検索結果から1つの動画をクリックして選択
7. 「選択中の曲」セクションに選択した曲の情報が表示されることを確認

### 期待結果
- ✅ YouTube検索が正常に機能する
- ✅ 検索結果が適切に表示される
- ✅ 動画選択が正常に機能する
- ✅ 選択した曲の情報が表示される

---

## テストシナリオ7: 地図への音楽投稿（ログイン必須）

### 目的
ログインユーザーが地図に音楽を投稿できることを確認

### 手順
1. Googleでログイン
2. 地図ページ（`/map`）にアクセス
3. YouTube音楽検索で曲を検索・選択
4. 位置情報が取得されていることを確認
5. 「この曲を地図に投稿」ボタンをクリック
6. 投稿成功メッセージが表示されることを確認
7. 地図上に新しいピンが追加されることを確認
8. 新しいピンをクリックして情報が正しく表示されることを確認

### 期待結果
- ✅ ログインユーザーのみ投稿ボタンが表示される
- ✅ 音楽投稿が正常に完了する
- ✅ MusicPinがDBに保存される
- ✅ 地図上に新しいピンが即座に表示される

---

## テストシナリオ8: いいね/嫌い機能（ログイン必須）

### 目的
音楽インタラクション機能が正常に動作することを確認

### 手順
1. Googleでログイン
2. プレイヤーページ（`/player`）にアクセス
3. 「いいね」ボタンが表示されることを確認（ログイン時のみ）
4. いいねボタンをクリック
5. 成功メッセージが表示されることを確認
6. 「嫌い」ボタンをクリック
7. 成功メッセージが表示されることを確認

### 期待結果
- ✅ ログイン時のみいいね/嫌いボタンが表示される
- ✅ いいね/嫌いが正常に記録される
- ✅ MusicInteractionがDBに保存される
- ✅ 同じ動画への重複インタラクションが適切に処理される

---

## テストシナリオ9: スワイプジェスチャー（モバイル）

### 目的
スワイプジェスチャーが正常に機能することを確認

### 手順
1. プレイヤーページをモバイルデバイスまたはブラウザのデベロッパーツール（モバイルモード）で開く
2. アルバムアート画像を右にスワイプ → いいね
3. 左にスワイプ → 嫌い
4. 上にスワイプ → 次の曲
5. 下にスワイプ → 前の曲

### 期待結果
- ✅ すべてのスワイプジェスチャーが正常に機能する
- ✅ ビジュアルフィードバックが表示される
- ✅ アニメーションがスムーズに動作する

---

## テストシナリオ10: Spotifyコード完全削除確認

### 目的
Spotify関連コードが完全に削除されていることを確認

### 手順（コードレビュー）
1. `grep -r "spotify" app/` でSpotify関連コードを検索
2. `grep -r "rspotify" app/` でRSpotify関連コードを検索
3. Gemfileに`omniauth-spotify`や`rspotify`が含まれていないことを確認
4. `config/initializers/rspotify.rb`が削除されていることを確認

### 期待結果
- ✅ Spotify関連コードが完全に削除されている
- ✅ RSpotify関連コードが完全に削除されている
- ✅ Spotify関連のGemが削除されている

---

## データベース検証

### MusicPinテーブル
```sql
SELECT COUNT(*) FROM music_pins WHERE pin_type = 'song'; -- YouTube曲
SELECT COUNT(*) FROM music_pins WHERE pin_type = 'legacy_spotify'; -- 旧Spotifyデータ
```

### MusicInteractionテーブル
```sql
SELECT interaction_type, COUNT(*) FROM music_interactions GROUP BY interaction_type;
```

### Userテーブル
```sql
SELECT provider, COUNT(*) FROM users GROUP BY provider;
-- 全ユーザーがprovider='google'であることを確認
```

---

## パフォーマンステスト

### YouTube API呼び出し数
- トレンド音楽取得: 1回/ページロード
- 検索: 1回/検索実行
- 動画詳細: 1回/動画選択

**目標**: 1日5,000リクエスト以下（YouTube APIクォータ内）

### ページ読み込み時間
- ホームページ: < 1秒
- プレイヤーページ: < 2秒（YouTube動画読み込み含む）
- 地図ページ: < 2秒（地図タイル読み込み含む）

---

## 成功基準

全テストシナリオが以下を満たす場合、Week 7完了とします:

- [ ] すべてのテストシナリオが成功
- [ ] Spotifyコードが完全に削除されている
- [ ] YouTube APIが正常に機能している
- [ ] Google OAuth認証が正常に機能している
- [ ] 地図投稿機能が正常に機能している
- [ ] データモデルが正しく動作している
- [ ] パフォーマンス目標を達成している

---

## トラブルシューティング

### YouTube API呼び出しエラー
- `.env`ファイルに`YOUTUBE_API_KEY`が設定されているか確認
- APIキーが有効か確認（Google Cloud Console）
- クォータ超過の可能性を確認

### Google OAuth認証エラー
- `.env`ファイルに`GOOGLE_CLIENT_ID`と`GOOGLE_CLIENT_SECRET`が設定されているか確認
- リダイレクトURIが正しく設定されているか確認（Google Cloud Console）

### 地図が表示されない
- Leaflet.jsが正しく読み込まれているか確認
- ブラウザコンソールでエラーを確認

### 位置情報が取得できない
- ブラウザの位置情報許可を確認
- HTTPSでアクセスしているか確認（localhostは例外）

---

生成日: 2025年1月

🤖 Generated with [Claude Code](https://claude.com/claude-code)
