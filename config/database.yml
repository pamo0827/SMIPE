# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem "sqlite3"
#
default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  primary:
    <<: *default
    database: storage/development.sqlite3
  cable:
    <<: *default
    database: storage/development_cable.sqlite3

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  primary:
    <<: *default
    database: storage/test.sqlite3
  cable:
    <<: *default
    database: storage/test_cable.sqlite3


# Store production database in the storage/ directory, which by default
# is mounted as a persistent Docker volume in config/deploy.yml.
production:
  primary:
    <<: *default
    database: <%= ENV.fetch("DATABASE_PATH", "/data") %>/production.sqlite3
  cable:
    <<: *default
    database: <%= ENV.fetch("DATABASE_PATH", "/data") %>/cable.sqlite3
  queue:
    <<: *default
    database: <%= ENV.fetch("DATABASE_PATH", "/data") %>/queue.sqlite3
  cache:
    <<: *default
    database: <%= ENV.fetch("DATABASE_PATH", "/data") %>/cache.sqlite3